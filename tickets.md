Make to do a drop down or like a thing that shows a couple of comments then there’s a button that shows more then shows and maybe sortable by clusters

Generate the clusters every time a new comment is added but only by adding the one comment to a cluster not by regenerating all clusters for all comments from scratch.
Gabriel got the vision

Hide various actions from different user classes.

Prompt to login / sign up when the user goes to submit comment. 

Automatically extract stuff from transcript remove the buttons

login isn't working